{
  "name": "@gaf/protocol-tools",
  "version": "0.1.0",
  "description": "Protocol JSON Schema, validator CLI, YAML->runtime compiler",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": { "gaf-protocols": "dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test",
    "validate": "node dist/cli.js validate"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@gaf/types": "workspace:*",
    "ajv": "^8.17.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9"
  }
}
