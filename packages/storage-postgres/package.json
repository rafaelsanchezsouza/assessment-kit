{
  "name": "@assessment-kit/storage-postgres",
  "version": "0.1.0",
  "description": "Reference storage adapter: Postgres repositories (ports & adapters, ADR-002) + local filesystem BlobStore",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test",
    "migrate": "node dist/migrate.js"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@assessment-kit/types": "workspace:*",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.20.4"
  }
}
